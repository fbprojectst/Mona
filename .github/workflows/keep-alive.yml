name: Keep Supabase Alive

on:
  schedule:
    # كل 3 أيام الساعة 00:00 UTC
    - cron: '0 0 */3 * *'
  workflow_dispatch: # يسمح بالتشغيل اليدوي أيضًا من تبويب Actions

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase project
        run: |
          curl -s "https://hjoqnnyfuiigfnlkzhqp.supabase.co/rest/v1/store_settings?select=id" \
            -H "apikey: sb_publishable_W9_E8jxV-TGstzdkbCB-kw_i5uJkBEv" \
            -H "Authorization: Bearer sb_publishable_W9_E8jxV-TGstzdkbCB-kw_i5uJkBEv"
